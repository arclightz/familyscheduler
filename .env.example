# Database
# For development, SQLite is recommended (simpler setup)
DATABASE_URL="file:./dev.db"

# For production, use PostgreSQL
# DATABASE_URL="postgresql://username:password@localhost:5432/familytask"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-here"

# ===================================================================
# AUTHENTICATION SETUP
# ===================================================================
# The app supports three authentication methods:
# 1. Email/Password (Development Only) - Works out of the box
# 2. Google OAuth - Requires Google Cloud Console setup
# 3. Microsoft OAuth - Requires Azure AD setup
#
# FOR DEVELOPMENT:
# - Leave OAuth credentials empty to use email/password authentication
# - Any email/password combination will work in development mode
# - Test users are seeded automatically (see prisma/seed.ts)
#
# FOR PRODUCTION:
# - Set up OAuth credentials below for secure authentication
# - Email/password authentication is disabled in production
# ===================================================================

# Google Calendar & OAuth (Optional for development)
# Setup guide: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Microsoft Graph & OAuth (Optional for development)
# Setup guide: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
MICROSOFT_CLIENT_ID=""
MICROSOFT_CLIENT_SECRET=""
MICROSOFT_TENANT_ID="common"  # Use "common" for multi-tenant or your tenant ID

# Email Notifications (Optional)
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER="your-email@example.com"
EMAIL_SERVER_PASSWORD="your-email-password"
EMAIL_FROM="Family Task Scheduler <noreply@familytask.app>"

# Environment
NODE_ENV="development"